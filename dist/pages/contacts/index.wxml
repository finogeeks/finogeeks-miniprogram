<block wx:if="{{$taroCompReady}}">
    <view>
        <view class="index">
            <block wx:if="{{showNavBar}}">
                <nav-bar compid="{{$compid__14}}"></nav-bar>
            </block>
            <scroll-view class="page_main" scroll-y="{{true}}" scroll-with-animation="{{true}}" scroll-top="0" style="{{anonymousState__temp}}" lower-threshold="20" upper-threshold="20" enable-back-to-top="{{true}}">通讯录</scroll-view>
        </view>
    </view>
</block>