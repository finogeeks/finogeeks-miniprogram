<block wx:if="{{$taroCompReady}}">
    <view class="list_item" bindtap="funPrivatepohdM" bindtouchstart="handleTouchStart" bindtouchend="handleTouchEnd" bindtouchcancel="handleTouchEnd" style="{{anonymousState__temp}}">
        <view class="left">
            <block wx:if="{{icon}}"><text class="icon">{{icon}}</text>
            </block><text class="text">{{name}}</text>
        </view>
        <view class="right">
            <block wx:if="{{statusText}}"><text class="text" style="{{anonymousState__temp2}}">{{statusText}}</text>
            </block>
            <block wx:if="{{hasNotif}}"><text class="notif_right"></text>
            </block>
            <block wx:if="{{hasRightArrow}}"><text class="arrow_right"></text>
            </block>
        </view>
    </view>
</block>